from setuptools import setup, find_packages

setup(
    name='employee_form',
    version='0.0.1',
    description='Employee Form App',
    author='Ali Al Mashyakhi',
    author_email='info@tags.sa',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)
